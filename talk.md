# 对话记录

> 项目：教学视频内容提取与总结工具
> 开始时间：2026-08-24

---

## 第一轮：项目代码阅读

**用户需求**：先把项目代码阅读一遍，开发文档在 Document 目录下，看完后告知，继续后面的对话。忽略 .git、.mypy_cache、venv、tests、scripts 目录。

**已阅读内容**：

### 代码文件
- `main.py` — CLI 入口，Windows GPU 路径设置，Pipeline 调用
- `config.py` — M1 配置管理，ConfigManager + 所有 Config dataclass，支持 .env / 环境变量覆盖
- `config.yaml` — 默认配置（豆包默认、FunASR、PaddleOCR、颜色过滤开启）
- `requirements.txt` — 依赖锁定（torch 2.13+cu126, paddlepaddle-gpu 2.6.1, paddleocr 2.8.1, funasr 1.4.2）

### core/ 核心模块（M2-M13）
- `pipeline.py` — M13 主流程编排，13 个阶段，顺序执行（注释标注可并行），含 ASR 纠错阶段
- `audio_extractor.py` — M2 音轨提取，ffmpeg 转 16kHz 单声道 WAV
- `frame_extractor.py` — M3 关键帧提取，ffmpeg fps 滤镜按间隔抽帧，支持单帧提取
- `asr.py` — M4 语音识别业务逻辑，缓存管理，通过适配层调用
- `ocr.py` — M5 文字识别业务逻辑，帧去重，颜色过滤预处理，缓存管理
- `text_merger.py` — M6 文本整理，纯 ASR 全文本（OCR 不混入）
- `knowledge_extractor.py` — M7 知识点提取，云端 LLM，极简输出（名称+时间戳）
- `problem_extractor.py` — M8 题目提取，云端 LLM + OCR 补充原题，含去重合并逻辑
- `screenshot_capture.py` — M9 题目截图，ffmpeg 单帧提取 + 颜色过滤
- `mindmap_generator.py` — M10 思维导图生成，OPML 2.0 格式
- `llm_client.py` — M11 LLM 客户端，多服务商管理，重试（指数退避），缓存
- `output_assembler.py` — M12 输出组装，格式化所有输出文件

### adapters/ 适配层（M15-M17）
- `asr_adapter.py` — M15 ASR 适配层，FunASRAdapter（paraformer-zh + fsmn-vad + ct-punc）+ Mock
- `ocr_adapter.py` — M16 OCR 适配层，PaddleOCRAdapter + Mock
- `llm_adapter.py` — M17 LLM 适配层，VolcengineAdapter（responses.create 接口，禁用思考）+ DeepSeekAdapter（chat.completions）+ Mock

### utils/ 工具集（M14）
- `models.py` — 统一数据模型（VideoInfo, Sentence, OCRResult, KnowledgePoint, Problem, ProcessResult, PipelineContext 等）
- `asr_corrector.py` — ASR 纠错工具，纯文本输入，按行对应时间戳，支持多服务商
- `image_preprocess.py` — 颜色过滤，去除彩色手写保留黑色印刷体
- `exceptions.py` — 自定义异常体系
- `file_utils.py` — 文件操作（ensure_dir, save_json, get_file_hash 等）
- `logger.py` — 日志配置（控制台 + 文件）
- `timestamp.py` — 时间戳格式化（秒 ↔ mm:ss / hh:mm:ss）
- `token_counter.py` — token 估算（中文 1.5 字符/token）
- `video_probe.py` — ffprobe 视频信息探测

### 开发文档
- `01_需求分析.md` — 项目定位、客户画像（初高中学生）、5 大痛点、功能需求拆分（F1-F5）、输出物定义、非功能需求
- `02_架构设计.md` — v2.0 纯云端架构，17 模块划分，数据流向（2 分流点 + 3 汇聚点），Mermaid 架构图，设计理由（6 个为什么），需求满足映射

---

## 项目理解总结

### 架构
流水线 + 并行分支，13 个处理阶段：
probe → extract_audio / extract_frames → asr / ocr → correct_asr → merge_text → extract_knowledge / extract_problems → capture_screenshots → generate_mindmap → assemble_output

### 关键设计决策
1. **纯云端 LLM**：放弃本地 4090 vLLM，默认豆包，备选 DeepSeek
2. **纯 ASR 逐字稿**：OCR 不混入全文本，避免语句逻辑混乱
3. **ASR 纠错**：豆包纠错后直接替换原文，不保留双版本
4. **颜色过滤预处理**：OCR 前去彩色手写，保留黑色印刷体题目
5. **M8 题目提取**：LLM 语义判断 + OCR 印刷体原题补充
6. **适配层模式**：ASR/OCR/LLM 均隔离第三方库

### 输出物
- `01_逐字稿.md`（ASR + 纠错）
- `02_知识点清单.md`（极简，名称+时间戳）
- `03_思维导图.opml`
- `习题/题目XX_原题.md` + `题目XX_解析.md`
- `截图/题目XX.jpg`

### 代码状态
所有模块已实现，含 Mock 适配器。已有示例输出（E01 集合概念，8 道题）。

---

---

## 第二轮：设计本意与开发问题

### 一、设计本意（原始构想）

#### 阶段1：音频与ASR
1. 从视频中截取音频
2. 通过ASR将中文语音转成文字，**保留原始文字**，用作debug分析

#### 阶段2：LLM粗提取（基于ASR文字）
将ASR文字交给LLM，分别提取：
- **知识点**：整个视频中有哪些知识点，将每个知识点讲解的文字内容截取出来，**保留这些文字**，用作debug
- **例题**：分别都讲了哪些例题，标注每个例题讲解开始的文字和结束的文字，**保留这些文字**，用作debug

#### 阶段3：精确定位（文字对比 + 时间戳）
分别提取知识点和例题：
- 通过**对比文字的方式**，结合ASR音频的时间戳，找到每个例题/知识点讲解的**起始时间和结束时间**，**保留这些时间戳**，用作debug
- 根据这个时间戳去提取视频帧，找到例题和知识点的**截屏**，**保留这些时间戳**，用作debug

#### 阶段4：内容整理
**知识点整理**：
- 将语音文字和OCR提取文字综合交给LLM，总结老师对于知识点的讲解
- 公式用LaTeX表示

**例题提取**（较复杂）：
- **第一，准确提取原题**：视频讲解过程中老师不会读题，题目往往出现在图像中，希望通过OCR提取出来形成笔记，**保留截图**用作debug
- **第二，提取老师的解题过程**：老师一边讲一边在板书，要求将ASR文字和图片OCR结合形成解题过程笔记，**保留截图**用作debug

---

### 二、开发中遇到的问题

#### 问题一：本地大模型成本节约计划失败

- **初衷**：为节约成本，计划使用本地大模型，部署在4090机器上，模型使用千问3.6-27B量化版
- **实际结果**：
  - 显存限制导致上下文受限，仅8K
  - 只能用截断方式处理长文本
  - 处理过程速度缓慢
- **后续考虑**：使用千问3.6-14B量化版，争取更大上下文和更快速度
- **当前状态**：暂时停用了本地LLM，改用云端

#### 问题二：知识点提取和文字提取精度很差

- **根因分析**：ASR的截取导致文字被时间戳截断，LLM在读文本时出现语义识别错误
- **解决方案：加入ASR纠错过程**
  - 将ASR生成的文本去掉时间戳，先整合成一个长文本
  - 让LLM纠正语法错误、用词错误和断句错误
- **拿回新文本后做两件事**：
  1. **保留新文本**用作debug，同时为对比不同LLM的纠错能力，文件名后增加大模型后缀
     - 最终发现：**豆包模型纠错结果好于DeepSeek**
  2. 将这个文本和ASR带时间戳的记录一并交给下游模块处理
- **下游模块**：定位到原本ASR，按照人的正常语速，按照文字的增加或减少重新定位时间戳

#### 问题三：题目识别和知识点识别错误频发

- **现象**：
  - 测试视频总是出现题目识别、知识点识别错误
  - 一个题目的讲解被截取成了2个题目
  - 题目内容也不对
- **已做的提示**：
  - 视频中的图片老师都是手写体
  - 老师会用圆形或方形的框标注重点
  - 题目一般都是黑色的印刷体
- **实际结果**：不太令人满意

#### 问题四：大模型调用流程优化期望

期望一次全文喂给LLM，能够返回3个东西：
1. **纠错后完整的文字**（用于debug）
2. **LLM划分的知识点**：有几个，每个知识点的文字段
3. **LLM总结的题目**：有几个，每个题目的文字段（**不区分题目和讲解**，这个交给后续模块处理）

---

---

## 第三轮：现状对比与差距分析

**用户问题**：现在的软件实现是按照我的本意实现的吗？

**结论**：不是。当前实现是简化版MVP，与设计本意有较大差距。

### 核心缺失的四块

1. **debug中间产物系统化输出**（原始文字段、定位时间戳、各阶段截图）
2. **文字对比+ASR时间戳精确定位机制**
3. **知识点深度整理**（OCR综合、讲解总结、LaTeX公式、知识点截图）
4. **解题过程ASR+OCR融合**

### 逐条差距详情

| 设计本意 | 当前实现 | 差距 |
|---|---|---|
| 保留ASR原始文字用作debug | 缓存JSON，输出只有纠错版 | 原始版未单独输出 |
| 截取知识点讲解文字段保留debug | 只输出知识点名称+时间戳 | 文字段缺失 |
| 标注例题讲解开始/结束文字保留debug | 提取原题+解题步骤，无原始文字段标记 | 文字段缺失 |
| 文字对比+ASR时间戳定位起始/结束时间 | LLM直接返回时间戳 | 定位机制缺失 |
| 知识点+例题截屏，保留时间戳debug | 只有题目截图 | 知识点截图缺失 |
| 知识点整理：ASR+OCR综合，LaTeX公式 | 只用ASR，只有名称 | OCR/讲解/LaTeX全缺 |
| 原题：OCR提取（老师不读题） | LLM提取后OCR补充 | 流程顺序不同 |
| 解题过程：ASR+OCR结合 | 只用ASR | OCR未参与 |
| 一次LLM调用返回纠错+知识点段+题目段 | 三次独立LLM调用 | 调用方式不同 |

---

## 第四轮：debug中间产物系统化输出（方案讨论）

**用户选择**：先讨论第1块——debug中间产物系统化输出。

### 核心思想
每个处理阶段都要保留人可读的中间产物，当最终结果出错时，可以逐级回溯定位问题根源。

### 8类debug产物清单

1. **ASR原始逐字稿**（带时间戳，人可读）
2. **纠错后全文**（多LLM版本对比，文件名加后缀，如 `_豆包.md`、`_deepseek.md`）
3. **知识点讲解文字段**（每个知识点截取的原文片段）
4. **例题讲解文字段**（每道题的开始文字和结束文字标记）
5. **定位时间戳记录**（文字对比+ASR时间戳定位的过程和结果）
6. **知识点截图**（根据定位时间戳截取的视频帧）
7. **例题原题截图**（OCR提取原题时的画面）
8. **解题过程截图**（老师板书推导过程的关键帧）

### 目录结构方案（待确认）

```
debug/{视频名}/
├── 01_asr原始.md              # ASR原始逐字稿（带时间戳）
├── 02_纠错_豆包.md             # 纠错后全文（豆包版）
├── 02_纠错_deepseek.md        # 纠错后全文（DeepSeek版，如做了对比）
├── 03_知识点文字段/
│   ├── 知识点01_集合的概念.md
│   └── ...
├── 04_例题文字段/
│   ├── 例题01.md
│   └── ...
├── 05_定位记录/
│   ├── 知识点定位.json
│   └── 例题定位.json
├── 06_截图/
│   ├── 知识点/
│   │   └── 知识点01_t=05m23s.jpg
│   ├── 例题原题/
│   │   └── 例题01_t=08m15s.jpg
│   └── 解题过程/
│       └── 例题01_步骤1_t=09m00s.jpg
└── 07_ocr全文.md               # OCR识别的所有文字汇总（带帧时间戳）
```

### 待确认的设计问题

1. **存放位置**：`output/{视频名}/debug/` 还是单独 `debug/{视频名}/`？
2. **开关控制**：加 `--debug` 参数控制，还是始终输出？
3. **与cache的关系**：`cache/` = 机器可读程序缓存（断点续传），`debug/` = 人可读分析产物，两者并存——是否合理？

**用户反馈**："很好"——认可8类产物清单和目录结构方案。

---

### 用户确认的设计决策

1. **存放位置**：单独 `debug/{视频名}/`（和 output 平级，干净分离）
   - **额外要求**：debug处理逻辑**单独成一个模块**，将来产品release时能直接删除而不会影响核心代码
   - 实现思路：新建 `debugger/` 模块（或 `core/debug_manager.py`），所有debug输出通过统一接口调用；核心流水线只调用 `debugger.save_xxx()`，不直接写文件；release时删除debugger模块 + 配置中关闭debug开关即可

2. **开关控制**：现阶段**始终输出debug**（不设开关，调试阶段一直需要）
   - 后续release时再加 `--debug` 开关或配置项

3. **纠错多版本**：
   - **不要多版本了，只保留豆包**作为默认纠错后端
   - **不删除DeepSeek适配器**，万一将来模型升级了可对比新模型能力
   - **配置文件中可自由选择每个LLM调用的服务商**——每个调用大模型的地方都能有配置，提供自由选择大模型的便利
   - 实现思路：在 `config.yaml` 的 `llm` 配置下，为每个任务类型（纠错、知识点提取、题目提取、思维导图）单独配置 `provider` 字段，默认都用 `volcengine`（豆包），可随时改为 `deepseek`

---

## 待续

debug模块设计已确认（独立模块 + 单独目录 + 始终输出 + 豆包默认但可配置）。
后续讨论：第2块（文字对比+ASR时间戳精确定位）、第3块（知识点深度整理）、第4块（解题过程ASR+OCR融合），或开始实现。

---

## 第五轮：第2块——文字对比 + 时间戳精确定位（设计讨论）

**用户要求**：讨论设计，代码实现细节不着急。

### 定位机制在流水线中的位置

```
ASR原始文字（带时间戳）
       │
       ▼
LLM一次调用返回三样东西：
  ① 纠错全文
  ② 知识点文字段（原文截取，无时间戳）
  ③ 题目文字段（原文截取，不区分题目/讲解）
       │
       ▼
文字对比 + 时间戳精确定位
  输入：LLM文字段（无时间戳）+ ASR原始文字（带时间戳）
  输出：每个知识点/题目的起止时间戳 + 定位过程记录（debug）
       │
       ▼
根据时间戳 → 截取视频帧（知识点截图 + 题目截图）
```

### 核心算法思路

1. **构建字符级时间戳映射表**：把ASR所有句子拼接成长文本，每个字符记录所属句子和时间戳范围
2. **模糊匹配**：对每个LLM文字段，在ASR长文本上滑动窗口，计算相似度（编辑距离/Jaccard/n-gram），找到最佳匹配位置
3. **起止时间**：起始字符查映射表得start_time，结束字符查映射表得end_time

### 用户确认的5个关键决策

**问题1：LLM返回的文字段是原文截取还是总结改写？**
- **原文截取**，不是总结改写
- 原因：①利用文字定位起止时间戳 ②原始截取文字供下一层模块做精细处理
- 总结改写是后面模块（第3、4块）的事情

**问题2：题目文字段"不区分题目和讲解"**
- 大部分例题讲解视频中，老师未必把题目完整读一遍，往往说"请看黑板"或"请看xxx"
- 所以仅靠文字段很难区分题目和讲解
- 题目和讲解的分离是后续模块（第4块）的事情，需要结合OCR

**问题3：匹配精度要求**
- **字符级精度**，句子级不行
- 原因：留给学生看黑板读题的时间可能仅1秒左右，老师就开始板书了
- 句子级精度会导致题目截图不准（截到老师已经开始板书的画面，题目已经被擦掉或遮挡）

**问题4：匹配失败降级策略**
- 方案A（报错跳过）**不可接受**
- 方案C（LLM返回全文百分比位置）**不好**：人说话有抑扬顿挫、节奏变换、停顿让学生思考，文字位置和时间戳差距大
- **倾向：降低相似度阈值再次匹配**，如果低于最低阈值都匹配不上，再用方案B（第一句话关键词粗略定位）

**问题5：和问题四（一次LLM调用）的关系**
- 流程顺序确认：
  1. 纠错、知识点提取、题目提取**合并成一次LLM调用**
  2. 返回的文字段作为定位机制的输入
  3. 定位完成后，再分别做**知识点深度整理（第3块）**和**题目原题/解题分离（第4块）**

### 待深入讨论

- 模糊匹配的具体算法选择（编辑距离 vs Jaccard vs 字符n-gram vs 其他）
- 字符级时间戳映射的构建方式（ASR是否提供字级时间戳？FunASR的paraformer-zh是否返回字级时间戳？）
- 降低阈值的阶梯策略（初始阈值多少？降几次？最低阈值多少？）

---

## 待续

第2块定位机制的核心策略已确认（原文截取 + 字符级精度 + 降阈值降级 + 一次LLM调用流程）。
后续：深入讨论定位算法细节，或继续第3块（知识点深度整理）、第4块（解题过程ASR+OCR融合）。

---

## 第六轮：第3块——知识点深度整理（设计讨论 + 重大反馈）

### 用户对第3块设计的反馈

#### 一、OCR关键帧截取策略（重大调整）

**当前实现**：M3 frame_extractor 按固定间隔（默认30秒）提取关键帧。

**用户要求**：
- 不能按30秒一帧截取
- 应该从时间起点按**1秒间隔**取帧
- 如果前后两帧图像没变化，则只保留前面的（变化检测去重）
- **去重算法需要深入讨论**（待定）

#### 二、OCR颜色过滤策略（调整）

**当前实现**：OCR识别前统一加颜色过滤，去除彩色手写，保留黑色印刷体。

**用户要求**：
- 知识点整理的OCR文字**不能加颜色过滤**
- 原因：颜色过滤会导致老师板书的关键信息丢失
- 推论：颜色过滤应该是**可配置、分场景**的——题目提取用颜色过滤（保留黑色印刷体题目），知识点整理不用颜色过滤（保留老师板书）

#### 三、LLM调用配置

- **大模型用豆包**：豆包模型在教育领域有积累
- **需要打开逻辑推理能力**：在大模型调用上注意开启此功能
- **消息格式差异**：接收消息部分注意不同API的消息格式差异
- **防止timeout**：要给大模型留出思考时间，防止等待过久产生timeout异常
- **先调研再编码**：仔细查阅API文档，对将要使用的API和对应模型调查清楚，形成文档，再开始编码。不要写了半天才发现代码写错了。

#### 四、输出结构修正

**原设计**：核心内容 / 公式 / 注意事项 三段式，公式单独列出。

**用户修正**：
- 不对，重点是**老师的分析问题和解决问题过程**
- 公式应该**合并在整个讲解过程中**，不单独列
- 输出结构应为：核心内容（公式嵌入其中）

#### 五、提示词修正

**原提示词**："不要做过度解读，只整理老师实际讲了什么"

**用户反馈**：
- 这个提示词不太好，因为存在两个精度问题：
  1. OCR的识别精度问题
  2. 老师板书手写识别问题
- 如果OCR能同时识别印刷体和手写体就最好，但不确定有没有这个功能，**需要再讨论**

#### 六、OCR使用方式

- 用户**非常认可**：ASR为主、OCR为辅，OCR用于补充公式和图表文字

#### 七、知识点截图

- **方案A**：截图只做debug/学生参考，不输入LLM
- **补充**：希望增加**豆包大模型自己对知识点的补充**——老师讲课过程中并未提及的内容，但注明知识范围**不要超出高考范围**（现阶段受众是中学生和中学生家长）

#### 八、Debug输出

- 用户觉得合理

---

### 代码重构整体要求（用户提出，适用于所有模块）

#### 重构原则

用户明确要求：功能升级了，架构也要大动，希望**代码重构**，而不是在原有基础上打补丁。

必须遵守以下设计原则：
1. **单一职责原则 (SRP)**：一个类应该只有一个引起它变化的原因
2. **开闭原则 (OCP)**：对扩展开放，对修改关闭
3. **里氏替换原则 (LSP)**：子类必须能够替换父类型
4. **接口隔离原则 (ISP)**：客户端不应该被迫依赖不使用的方法
5. **依赖倒置原则 (DIP)**：高层模块不依赖低层模块，两者都依赖抽象；面向接口编程，不面向实现编程
6. **迪米特法则 (LoD)**：一个对象对其他对象有最少了解
7. **合成复用原则 (CARP)**：优先使用对象组合/聚合，而不是继承

#### 编码风格要求

1. **一个文件一个class**：不喜欢interface和implement都在一个文件内的风格，文件太长查看代码麻烦
2. **不要裸 `try except Exception as e`**：要仔细阅读第三方API文档，对不同的exception分别处理
3. **避免硬编码**：例如 `default_config` 里写死model名称，model应该从配置来
4. **adapter设计**：为了适配不同第三方库，底层LLM的变化要求设计adapter（当前已有，继续保持）

#### 问题追踪机制

用户希望有地方记录代码问题，做一个**问题追踪**，防止下次再犯同样的错误。

---

## 待续

第3块设计收到重大反馈，需后续处理：
1. 豆包API调研（逻辑推理能力、消息格式、timeout处理、模型列表）——形成文档
2. OCR关键帧截取策略调整（1秒间隔+变化检测）——去重算法待深入讨论
3. OCR颜色过滤分场景配置
4. OCR印刷体+手写体识别能力调研
5. 代码重构整体规划（SOLID+LoD+CARP，一个文件一个class，异常分类处理，去硬编码）
6. 创建代码问题追踪文档
7. 第4块（解题过程）待讨论

---

## 第七轮：R-001 & R-002 调研完成

**用户要求**：进行 R-001（豆包API调研）和 R-002（PaddleOCR手写体能力调研），形成调研报告存在 Document 目录内。

**完成产出**：`Document/开发文档/10_API与第三方库调研报告.md`

### R-001 豆包API 关键发现

1. **统一使用 Responses API**（`/api/v3/responses`），放弃 Chat API
2. **深度思考模型必须用流式调用**（`stream=True`），非流式容易超时
3. 响应包含两部分：`reasoning`（思维链摘要）+ `output_text`（最终回答）
4. 控制参数：`thinking.type=enabled/disabled`，`reasoning.effort=low/medium/high`
5. 2.1系列模型默认 `reasoning_effort=high`
6. `max_output_tokens`（不是 `max_tokens`）= 思维链 + 回答长度
7. 多模态输入：`input` 数组格式，`content.type` 必须是 `input_text`（不能写 `text`）
8. 错误码分类：400参数错误(不重试)、401认证失败(不重试)、429限流(指数退避)、500服务端错误(延迟重试)、超时(改流式)
9. 当前代码问题：非流式调用、thinking硬编码disabled、max_tokens命名错误、裸except Exception
10. 可用模型：doubao-seed-evolving(1024k)、doubao-seed-2-1-pro(256k)、doubao-seed-2-1-turbo(256k)等

### R-002 PaddleOCR手写体 关键发现

1. PaddleOCR 3.0 + PP-OCRv5（2025年5月发布）**原生支持手写体**，单模型同时支持印刷体+手写体，精度提升13%
2. 当前项目 2.8.1 默认模型不支持手写体，但有专用模型 `ch_PP-OCRv4_handwritten_rec_infer` 可替换
3. 短期方案：保持2.8.1，适配层支持可切换识别模式（PRINT_ONLY / HANDWRITTEN_ONLY / HYBRID）
4. 长期方案：评估升级到 PaddleOCR 3.0
5. 场景配置：题目提取用印刷体+颜色过滤；知识点整理和解题过程用混合模式+无颜色过滤
6. 混合模式：两个模型分别识别后按位置去重合并

---

## 待续

R-001/R-002 调研完成。后续可继续：
- 讨论 OCR 关键帧截取策略（1秒间隔+变化检测去重算法）
- 讨论第4块（解题过程：原题OCR提取 + ASR+OCR融合解题过程）
- 讨论整体代码重构架构设计
- 开始推演文字对比定位算法细节（08号文档中的8个待定事项）

---

## 第八轮：GitHub Issues 问题追踪建立

**用户要求**：将代码问题追踪迁移到 GitHub Issues 管理，后续可继续增加 issue，issue 的解决先不着急，文档确认后再通知解决并关闭。

**仓库信息**：`toddhuang/videocontents`，GitHub CLI 已登录（账号 toddhuang，repo 权限）

**已创建自定义标签**：
- `refactor`（代码重构，黄色）
- `research`（待调研事项，绿色）
- `architecture`（架构设计问题，蓝色）
- 复用默认标签：bug、enhancement、documentation、question 等

**已创建 15 个 Issues（#2–#16）**：

| 编号 | 标题 | 标签 |
|---|---|---|
| #2 | [C-001] 一个文件内包含多个 class，需拆分为一个文件一个 class | bug, refactor |
| #3 | [C-002] 裸 try except Exception 捕获所有异常，需分类处理 | bug, refactor |
| #4 | [C-003] 硬编码配置值，未从配置文件读取 | bug, refactor |
| #5 | [C-004] 模块间直接依赖具体实现，未面向接口编程 | refactor, architecture |
| #6 | [A-001] OCR关键帧按固定30秒间隔提取，需改为1秒间隔+变化检测去重 | architecture, enhancement |
| #7 | [A-002] OCR颜色过滤全局开启，需改为分场景配置 | architecture, enhancement |
| #8 | [A-003] LLM调用分散为多次独立调用，需合并为一次调用返回三样东西 | architecture, enhancement |
| #9 | [A-004] 知识点提取只有名称+时间戳，需增加深度整理 | architecture, enhancement |
| #10 | [A-005] 缺少文字对比+ASR字级时间戳精确定位机制 | architecture, enhancement |
| #11 | [F-001] 缺少独立的debug模块和debug输出体系 | enhancement |
| #12 | [F-002] 缺少知识点截图 | enhancement |
| #13 | [F-003] 解题过程未结合OCR，需ASR+OCR融合提取解题过程 | enhancement |
| #14 | [R-001] 豆包（火山引擎）API详细调研（已完成调研报告，待确认后关闭） | research |
| #15 | [R-002] PaddleOCR手写体识别能力调研（已完成调研报告，待确认后关闭） | research |
| #16 | [R-003] 帧变化检测去重算法选型（待调研） | research |

**用户工作方式**：
- 后续可继续增加 issue
- issue 的解决先不着急
- 文档确认后，用户通知哪些 issue 解决并关闭

---

## 第八轮：Issue #4 LLM 接口设计讨论（2026-08-26）

### 讨论起点

Issue #4 [C-003] 要求消除硬编码配置值。初步计划是把适配器中的 default_config 和业务模块中的 temperature/max_tokens 移到 config.yaml。

### 核心转折：LoD 违规

用户指出：max_tokens 是 LLM 底层限制，上层不应该知道。上下文超限处理应该由 LLM adapter 内部处理，换 LLM 不应该导致所有调用点都要改。这违反迪米特法则（LoD）和依赖倒置原则（DIP）。

### 全局 LoD/DIP 检查结果

| 问题 | 严重度 | 处理决定 |
|---|---|---|
| LLM 参数暴露给上层 | 🔴 严重 | 本次重点处理 |
| ffmpeg/ffprobe 直接调用 | 🔴 严重 | **不处理**——功能直接绑定 ffmpeg，无可替代，不过度设计 |
| OCR 颜色过滤在上层 | 🟡 中等 | 本次忽略 |
| 视频探测工具分散 | 🟢 轻微 | 本次忽略 |

### LLM 接口质疑

用户指出当前 `LLMAdapter` 的 `chat`/`chat_stream` 接口本身就是暴露细节：

```python
def chat(messages, temperature=0.3, top_p=0.9, max_tokens=2000, **kwargs)
```

要求：
1. 重新统计 LLM 使用场景
2. 根据使用场景重新定义接口
3. 讨论哪些逻辑该由 LLM 处理，哪些属于上层

### 功能边界整理

**LLM 的功能**：文本生成服务——接收 messages，返回文本。负责模型调用、模型参数管理、上下文超限处理、重试/超时/错误恢复、流式/非流式。

**各业务模块的功能**：

| 模块 | 功能 | 上层自己做的事 |
|---|---|---|
| ASR 纠错 | 纠正 ASR 文本并对齐时间戳 | 构建提示词、解析结果、时间戳对齐 |
| 知识点提取 | 从全文识别知识点 | 构建提示词、解析 JSON、转 KnowledgePoint |
| 题目提取 | 从全文识别例题 | 构建提示词、解析 JSON、转 Problem、去重合并 |
| 思维导图 | 把知识点组织成思维导图 | 构建提示词、解析 OPML |
| 健康检查 | 检测 LLM 服务可用性 | 发送最短请求，返回 True/False |

### 参数归属逐点讨论结论

#### 1. temperature / top_p

- **temperature**：控制生成随机性的参数。低温=确定保守，高温=多样创造。不是模型固有属性，是每次调用根据任务需求设定的。
- **top_p**：核采样，截断低概率候选词集合，不重新加权。与 temperature 机制不同但效果类似。
- **关键结论**：不同任务确实需要不同的随机性（纠错要确定，创意要多样），这是当前大模型能力的局限性。但"任务需要确定性输出"是**任务意图**，"用 temperature=0.1 还是 top_p=0.9 实现"是**模型参数**。从意图到参数的映射应由 LLM 层负责，上层不应感知这些参数。
- **另外**：temperature/top_p 不是所有模型都支持，具体支持情况取决于模型和 API，进一步说明应归 LLM 层控制。

#### 2. thinking / reasoning

- 不同模型实现方式不同：豆包用 `thinking.type`，OpenAI 用 `reasoning_effort`，DeepSeek 靠模型名区分，有些模型不支持。
- **结论**：是 API 实现细节，不应暴露给上层。哪些任务需要推理是任务意图，不是 API 参数。达成共识。

#### 3. stream

- stream 的设计初衷是**实时消费输出**：聊天逐字显示、提前终止、边生成边送 TTS、降低感知延迟。
- 在本项目（批处理工具）中，上层只需要完整结果，没有实时消费需求。
- "长输出用流式防超时"是技术手段，不是业务需求。
- **不需要预判输出长度**：没有 API 能预知输出长度，也不需要预判——LLM 层统一走流式接收、内部拼接完整结果后返回上层即可。流式不增加费用、不改变结果。
- **结论**：stream 是 LLM 层内部实现选择，不应暴露给上层。

### 待继续讨论

- 输出长度需求（max_tokens 的任务属性部分）如何处理
- model/provider 选择如何处理
- presence_penalty/frequency_penalty、stop 等参数
- 接口最终形态
- 长文本分段的归属
- 上下文超限的具体处理策略

---

## 待续
