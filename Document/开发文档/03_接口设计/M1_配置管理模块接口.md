# M1 配置管理模块 — 接口设计

> 所属文档：02_架构设计.md
> 模块编号：M1
> 模块职责：加载/校验配置，集中管理所有参数

---

## 一、模块概述

配置管理模块负责加载 YAML 配置文件，校验配置参数合法性，提供统一的配置访问接口。管理敏感信息（API Key 从环境变量读取，不硬编码）。采用 `ConfigManager` 类式接口，支持点号路径访问。

---

## 二、接口列表

### 2.1 ConfigManager.load

**功能**：加载并解析 YAML 配置文件，返回 Config 对象。

**签名**：
```python
class ConfigManager:
    def load(self, config_path: str) -> Config
```

**输入参数**：
| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| config_path | str | 是 | 配置文件路径（YAML 格式） |

**输出**：Config 对象

**异常**：
| 异常类型 | 触发条件 |
|---|---|
| FileNotFoundError | 配置文件不存在 |
| ConfigValidationError | 配置参数校验失败 |
| yaml.YAMLError | YAML 解析失败 |

---

### 2.2 ConfigManager.get

**功能**：获取配置项的值，支持点号路径访问（如 `config.get("llm.cloud.base_url")`）。

**签名**：
```python
def get(self, key: str, default: Any = None) -> Any
```

**输入参数**：
| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| key | str | 是 | 配置项键名，支持点号路径 |
| default | Any | 否 | 键不存在时返回的默认值 |

**输出**：配置项的值

**异常**：无（键不存在时返回 default）

---

### 2.3 ConfigManager.validate

**功能**：校验配置参数的合法性（必填项、类型、范围、依赖关系）。

**签名**：
```python
def validate(self, config: dict) -> bool
```

**输入参数**：
| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| config | dict | 是 | 配置字典（YAML 解析后的原始字典） |

**输出**：bool（校验通过返回 True）

**异常**：
| 异常类型 | 触发条件 |
|---|---|
| ConfigValidationError | 校验失败，异常信息包含具体错误字段和原因 |

**校验规则**：
- `llm.cloud.api_key`：必须存在（或环境变量 `DEEPSEEK_API_KEY` 已设置）
- `llm.local.base_url`：必须是合法 URL
- `video.frame_interval`：必须 > 0
- `output.timestamp_format`：必须是 "mm:ss" 或 "hh:mm:ss"
- `asr.adapter_type`：必须是已注册的适配器类型（如 "funasr"）
- `ocr.adapter_type`：必须是已注册的适配器类型（如 "paddleocr"）

---

## 三、数据结构

> 共享数据结构统一定义见 `00_数据模型.md`，此处仅列配置相关结构。

### 3.1 Config（dataclass）

```python
@dataclass
class Config:
    video: VideoConfig
    llm: LLMConfig
    asr: ASRConfig
    ocr: OCRConfig
    output: OutputConfig
    paths: PathsConfig
    cache: CacheConfig
```

### 3.2 子配置结构

```python
@dataclass
class VideoConfig:
    audio_sample_rate: int = 16000
    audio_channels: int = 1
    frame_interval: int = 30
    scene_change_detection: bool = False
    scene_change_threshold: float = 0.3

@dataclass
class LLMConfig:
    local: LocalLLMConfig
    cloud: CloudLLMConfig
    max_retries: int = 5
    connect_timeout: int = 10
    stream_chunk_timeout: int = 30
    cache_enabled: bool = True
    health_check_interval: int = 30  # 健康检查间隔（秒）

@dataclass
class LocalLLMConfig:
    adapter_type: str = "vllm"
    base_url: str
    model: str
    context_length: int = 8192
    temperature: float = 0.3
    top_p: float = 0.9

@dataclass
class CloudLLMConfig:
    adapter_type: str = "deepseek"
    base_url: str
    model: str
    context_length: int = 131072
    api_key: str  # 从环境变量读取，不硬编码
    temperature: float = 0.3
    top_p: float = 0.9

@dataclass
class ASRConfig:
    adapter_type: str = "funasr"
    model: str = "paraformer-zh"
    punc_model: str = "ct-punc"
    vad_model: str = "fsmn-vad"
    batch_size_s: int = 300
    device: str = "cuda"

@dataclass
class OCRConfig:
    adapter_type: str = "paddleocr"
    use_angle_cls: bool = True
    lang: str = "ch"
    det_db_box_thresh: float = 0.5
    use_gpu: bool = True

@dataclass
class OutputConfig:
    transcript_filename: str = "01_逐字稿.md"
    knowledge_filename: str = "02_知识点清单.md"
    mindmap_filename: str = "03_思维导图.opml"
    problems_dirname: str = "习题"
    screenshots_dirname: str = "截图"
    timestamp_format: str = "mm:ss"

@dataclass
class PathsConfig:
    cache_dir: str = "./cache"
    output_dir: str = "./output"
    temp_dir: str = "./temp"

@dataclass
class CacheConfig:
    """缓存管理配置"""
    enabled: bool = True
    max_cache_size_gb: int = 400  # 缓存空间上限（GB），超过时按 LRU 清理
    ocr_frame_cache: bool = True   # 是否缓存 OCR 原始帧图片（MVP阶段为保证不丢失信息，默认开启）
    llm_cache: bool = True          # LLM 响应缓存
    asr_cache: bool = True          # ASR 结果缓存
```

---

## 四、第三方库调用

| 库 | 用途 | 调用方式 |
|---|---|---|
| PyYAML | YAML 配置文件解析 | `yaml.safe_load(file)` |
| python-dotenv | 环境变量加载（API Key） | `load_dotenv()` + `os.environ.get()` |
| dataclasses | 配置数据结构 | `@dataclass` 装饰器 |

---

## 五、依赖关系

- **被依赖**：所有模块都依赖配置管理模块
- **依赖**：无（底层模块）
